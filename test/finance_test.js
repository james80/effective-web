'use strict';

describe('myApp.finance module', function() {

    beforeEach(module('myApp.finance'));

    describe('finance controller', function () {

    it('should ....', inject(function($controller) {
        //spec body
        var view2Ctrl = $controller('FinanceCtrl');
        expect(view2Ctrl).toBeDefined();
    }));
  });
});